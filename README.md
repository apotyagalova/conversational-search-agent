# search_agent

#web server run:
rasa run --model models --enable-api --cors "*" --debug

#rasa server run:
rasa run actions

#interface rendering:
app.py

#image or video archive server:
python -m http.server 8000
 
